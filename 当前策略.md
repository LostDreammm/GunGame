# 《未来战争》当前策略总览

> **本文档 = 当前已落地的对战策略手册**（截至 2026-09-24，代码已按本手册重写：14 墙 C 环、CP 单人守夜、三角色、采矿锁与白天买券夜间用券）。
>
> 写给「怎么打、谁干什么、哪一天做什么」。参数真源仍是 `STRATEGY_DECISIONS.md`，规则事实见 `RULE_ASSUMPTIONS.md`，工程进度见 `PROGRESS.md`。
>
> 一句话：**先保证基地比对方活得久，再用 3 火箭单人守夜 + 双工人造血，白天拿任务分，中期武器优先滚雪球。**

---

## 1. 怎么赢

### 1.1 胜负顺序（硬约束）

1. **基地先被毁 = 直接判负，无视积分。** 一切经济、任务、击杀都建立在「基地活得比对方久」之上。
2. 双方都活着（或同时被毁）才比积分。
3. 异常累计 5 次 → 代码停调度 ≈ 慢性死亡（工程红线，不是策略问题）。

总积分 = 任务分(score1) + 击杀分(score2) + 生存分(score3)。10 天全活生存分约 **550**，是积分大头；击杀与任务各约 200–400（量级随夜潮/任务形态浮动）。

**优先级：生存 > 击杀 ≈ 任务 > 宝藏。** 生存既是分数本身，也是后两类分数的乘数。为多采几回合矿、多做一个任务而牺牲基地血量，只有防线 TTF 足够安全时才值得。

### 1.2 总基调

采用 **方案 B：平衡流**（Bootstrap → 经济扩张 → 中期升级），证据驱动后再放开激进度。

| 已拍板 | 内容 |
|---|---|
| 武器 | **3 火箭**，不造电磁/加特林 |
| 夜防 | 开拓者一站控 3 炮，利用 3 回合冷却轮转（每回合 1 发） |
| 墙 | 14 格三侧环，正面+两翼，开口/炮台背向来敌 |
| 工人 | 夜 1–3 全力采矿卖钱，不蹲防；Day4+ 或墙重伤才设修墙岗 |
| 任务 | Day1 起接，确定性路径优先，LLM 仅兜底 |
| 宝藏 | 框架已写，仅推断就绪才行动，最低优先 |
| PvP 召唤令 | **关闭** |

明确不做：20 墙全封、第一夜裸奔、电磁/加特林入编、群体 CRITICAL 召回、整夜粘性回家锁、友伤回避惩罚、跑敌方侧矿。

---

## 2. 三角色（按 ID 固化）

| 角色 | 绑定 | 白天 | 夜间 |
|---|---|---|---|
| **炮手** | 开拓者 | 任务 / 买武器券 / 插空升级武器 / 入夜前归位 CP | 控 3 火箭；兵潮清空且距天亮 >30 回合可外出任务/买券 |
| **修理工** | worker[0]（ID 最小） | D1 石料+建墙；D2–D3 补墙缺口→采铜铁卖钱买券（常备 5 石头）；D4+ 黄昏预留回墙内 | D4+ 就位 `repair_post` 升级/抢修；D1–D3 无重伤则继续采矿 |
| **挖矿工** | worker[1] | 采矿→卖钱→返回，循环到底 | 同左；贴脸才规避，脱离即继续采 |

**炮手优先级：** 吃药 > 买武器券 > 升级武器 > 入夜前归位 > 做任务 > 接任务 > 寻宝。

**修理工优先级：** 吃药 > 建墙/补缺口 > 采购券与 WallFixer > 夜间升级/抢修 > 采矿。

**挖矿工优先级：** 吃药 > Day1 石头>20 且墙未满时紧急帮建 > 持续外出采矿（躲机器人、判路径）。

工人背包不共享：修墙券和 WallFixer 必须在修理工自己包里。

---

## 3. 十日作战时间表

### Day1（Bootstrap，硬门槛）

目标：入夜前 **14 墙环 + 3 火箭** 成型。初始 75 金正好 3 座，必须当天建完，否则火力永久落后。

- 修理工专职采石建墙：一次尽量采 12–14 块再连续建（不采一建一）。每座矿只有 10 块 → 采满一座且没有下一座石矿时立即开建，防止「凑不满 14 就永远不开建」。
- 挖矿工采铜/铁卖钱；**不共享石矿**。仅当按进度单工人建不完 14 墙时，挖矿工才临时帮建；他自己包里石头 >20 且墙未满也可紧急帮建。
- 开拓者不远离基地，熟悉地图；有预算则开始接第一个任务。
- 建墙顺序：**迎敌正面（离 CP 最远）最优先**，再到两翼。武器优先于墙派单。

### Night1

- 开拓者站 CP，三人名义上只有他开火：轮转 3 火箭。
- 双工人夜间全力采矿卖钱，**不设修墙岗、不群体召回**。
- 记录出生点（全局单点、每晚固定，一次观测终身有效）和夜潮组成，校准 FRONT。

### Day2–Day3（造血）

- 墙环若有缺口：修理工优先补建（建造失败格 40 回合后重试，不再永久拉黑）。
- 经济转铜/铁；选矿按**单位回合收益**，只采我方侧近矿。
- 白天只**买券**，夜间才用券升级/修复（白天最大化采集；升级=回满血）。
- 开拓者专职任务；任务途中（TASK_TRAVEL）也可插空升级武器。
- 金币优先把 3 火箭推到 L2，再考虑墙。

### Night2–Night3

- 仍是单人守炮 + 双工人夜采。墙严重受损（≥3 面掉血或有墙 <50%）才临时设修墙岗。
- 工人贴脸（≤3 格）才规避：基地安全则直接远离机器人，不迎着兵潮往家跑；基地危险（机器人距基地 ≤4）才撤内圈。脱离到 ≥5 格恢复采矿。

### Day4+（中期滚雪球）

- 修理工白天预留回合回墙内；一旦开始返回，**跨昼夜粘性**走到墙内（A* 绕墙，40 回合兜底放弃）。
- 夜间修理工就位 `repair_post`（内圈邻墙最多的可站格），上下走动升级/抢修，不再只「回家待命」。
- 抢修口径：墙血 **<50%** 才修，正面优先；L1/L2 用升级券（回满血+提上限），**L3 只能用 WallFixer**。
- 武器继续推 L3；墙全到 L2 后才允许墙 L3（禁止 L3 邻 L1）。
- 金币 ≥250 才考虑基地升级（升级兼回满血，是后期保险）。

### 全场贯穿

- 官方新闻影响矿价/停工：停工前囤该矿，停工期优先高价卖，恢复后清空预测。
- 民间传闻逐日累积，推断宝藏就绪后才献祭（最低优先）。
- 召唤令 PvP 全程关闭。

---

## 4. 布局（BaseLayout）

蓝区 = 距基地切比雪夫 1（建武器，12 格）；黄区 = 距基地 2（建墙，20 格）。墙只用三侧约 **14 格**，开口侧不建，后门不封。

**FRONT = 开口 / 炮台 / 通道侧，背向来敌。** 墙环朝图心迎敌：

- challenger（左上）→ 机器人从东来 → 墙在东/南/北，开口朝西
- defender（右下）→ 机器人从西来 → 墙在西/南/北，开口朝东

以开口朝西为例：

```
|空白|围墙|围墙|围墙|围墙|围墙|
|空白|空白|空白|空白|空白|围墙|
|空白|炮台|基地|基地|空白|围墙|
|空白|  CP |基地|基地|空白|围墙|
|空白|炮台|炮台|空白|空白|围墙|
|空白|围墙|围墙|围墙|围墙|围墙|
```

- 3 炮台与控制点 CP 两两相邻，开拓者一站控三炮；凑不齐 3 炮时自动迁移 CP。
- 火箭无视阻挡，隔墙打牛；墙的作用是聚怪、挡伤害，不是挡弹道。
- `repair_post`：内圈、非炮台/非 CP、邻墙最多的可站格，供修理工夜间抢修。

---

## 5. 经济

Gold 不直接给分，买的是生存和击杀效率：武器 25、武器券 100/150、墙券 20/30、修墙包 10、药 10。防线没成型前每一金都关键；成型后多余的金才升级。

### 5.1 采矿

- **Mine Lock**：锁定一座矿采完（10 次或 zone 消失）再走，禁止采几下换矿。
- 选矿评分：**rate = 矿价 × 10 / (2 × 路程 + 10)**，同矿种近矿必胜远矿。
- 硬约束：优先距我方基地 ≤16 格；无近矿只接受**我方侧**；绝不深入敌方侧（宁可空闲等刷新）。
- **安全矿（B 方案）**：机器人行军走廊 = 出生点 → 我方基地连线（半宽 5），只算距基地 >8 的部分。矿落在走廊内，夜间/黄昏不采。
- 经济岗优先铜/铁；石矿仅无铜铁时兜底。prefer=money 时若锁着石矿，释放改采铜铁。
- 连续 3 次 collect 背包不涨 → 解锁并拉黑该矿 20 回合。采集失败拉黑 10，再犯 40。

### 5.2 卖货

- 背包达容量 **60%** → 立即去卖，不限距离（防止金币长期停滞）。
- 顺路：小贩 ≤3 格且货值 ≥8。
- 专程：货值 ≥25 且小贩 ≤20 格。
- 强制：背包满，或急用金（武器待重建且金 <25）。
- 白天晚上都可采可卖。夜间矿/小贩距活机器人 ≤3 格则不采不卖。

### 5.3 夜采哲学

夜潮主攻基地，杀工人只是顺路（攻击挡路单位），不会绕路追杀。因此：

- 取消群体 CRITICAL 召回和整夜粘性回家锁（这两种都会把经济打崩）。
- 危险圈收得很紧（贴脸 3 格），滞回脱离后立刻回去干活。
- 血量换经济：安全阈值内允许墙/基地掉血，换采集回合。

---

## 6. 夜防与火力

### 6.1 编制

开拓者单人值守。每回合至多操控 1 座且不能边走边打：站在 CP 上轮转 R1→R2→R3，吃满冷却空档，形成持续火力。移动回合不开火，控炮回合不用药。

机器人贴到 CP ≤1 格才撤内圈——过早撤 = 整夜哑火。HP≤120 且有药则吃药（不占武器指令时）。

### 6.2 开火规则

- 射程内有冲我方的机器人时，**只打我方批次**（`targetTeam`），不帮对面清潮。
- **无友伤惩罚**：贴脸必须开火。墙聚怪 + 火箭隔山打牛是核心玩法。
- 落点评分：中心 20 + 溅射 10/只 + 致命 +15/只 + 逼近加权（≤12 格）+ 我方向 +5。低于 20 分不开火，省 3 回合冷却。
- 威胁等级（TTF）只用于记录和是否设修墙岗，不再触发全员召回。

### 6.3 夜潮量级（已观测样本）

Day1 夜约 60 小 + 10 中（70 只量级）；此后含大怪，L1 火箭无法独立清场，**墙环是刚需**。次日早上残余自动清除。

---

## 7. 升级与维修

升级 = **回满血 + 提升上限**，比单纯 WallFixer 更值钱。因此：**白天买券，夜间用券。**

### 7.1 总序

1. 武器全部 L2（未完成前暂停墙升级，只修血量 <30% 的临界墙，给 100 金武器券让路）
2. 受损墙 L1→L2（正面优先）
3. 补建墙 / 正面墙 L2（前夜被拆、次日补建的墙回到 L1，优先重升）
4. 武器 L3
5. 其余墙到 L2
6. 墙 L3（仍有 L1 墙时禁止任何墙升 L3）
7. 基地（金 ≥250）
8. Day3+ 按需备 WallFixer

每回合最多派 **1 个**升级任务，始终留一个工人在外挖矿。武器升级可以抢占采矿锁。到店一次买齐当前所有待升武器所需券（V1×L1 数 + V2×L2 数）。

### 7.2 围墙状态表（WallRegistry）

跨回合记录每面墙的存在/等级/血量/是否正面/被攻破回合/补建回合。修复与升级都读这张表，不靠「本回合看见什么」。

### 7.3 备货

- 修理工常备 **5 块石头**（卖石时留下）。
- WallFixer：Day3+ 工人背包至少 2 个；Day4+ 或已有 L3 墙时按 L3 墙数补，上限 3。
- 买不起返回 0，不发非法 `buy`。

---

## 8. 自进化任务

开拓者专职。每方 2 个任务点，完成后约 30 回合冷却。真实超时通常只有 **10–15 回合**，必须快。

### 8.1 接取与红线

- 两任务点交替，优先与上次不同类型；黄昏前必须算上走路+做题+回家。
- **`acceptTask` 失败立即放弃，终身回避该点。** FAIL 计 errorCode 4，重试循环 = 封号。
- 同文本任务再次接取时，会话必须重置，禁止串场。

### 8.2 求解原则：确定性优先，LLM 兜底

任务期 LLM **不限次、不占每日 3 次额度**。非任务期（新闻/宝藏）每天最多 3 次，且每回合至多 1 条。

标准路径：

1. **健壮探索（单命令）**：定位任务文件 → 读任务书 → **顺着任务书再读它引用的二级文档**（`API_DOCS.md` / `spec.md` / README / `ws_*`）→ 列权限。
2. **工程类**：读 spec → 去 CRLF → `chmod` → `./check` → 按 FAIL/spec 修复 → 再 check → 行首 `TOKEN: xxx`（长度≥6、排除占位符）直接提交。禁止猜 token。
3. **API 类**：按文档调用；确定性探测「认证 × 参数名 × 城市」矩阵，命中 200 即锁定，禁止横跳。非 2xx / `status:error` **禁止解析成 0/空**。收割后**强制提交**。
4. 两条确定性路径都走不通，才进 LLM 循环。上限 `min(8, timeout-2)`，连续 3 次非 JSON 强制结束。
5. LLM 命令未显式 `cd` 且非绝对路径 → 自动补工作目录；漏 `cd` 会直接跑飞。

完成前 2 个任务后提取 SOP，次日同类任务复用。

---

## 9. 宝藏与新闻

**新闻：** 正则解析官方消息（矿种 + 停工/塌方/检修 + 天数）。停工前囤货、停工期高价卖、恢复后清空。正则失败才用每日额度里的 1 次 LLM。

**宝藏：** 民间传闻逐日累积 → LLM 推断 `{x, y, items, day, ready}` → 仅 `ready` 才买祭品、去祭坛、`summonTreasure`。最低优先：有任务可接时不做。献祭数量必须刚好，动作合法即消耗物品。

---

## 10. 关键参数速查

与代码常量对齐；调参改这里对应的代码，再回写 `STRATEGY_DECISIONS.md`。

| 域 | 参数 | 当前值 |
|---|---|---|
| 建造 | Day1 采石批量 | `min(剩余墙, 14)`；采满一座(≥10)且无下座石矿则开建 |
| 建造 | 非 Day1 批量 | `min(6, 剩余墙)` |
| 建造 | 入夜紧急建墙 | 剩余白天 ≤ 待建墙 + 6 |
| 经济 | 背包即卖 | 容量 60% |
| 经济 | 近矿半径 | 16 |
| 经济 | 行军走廊 | 半宽 5，距基地 >8 才算危险 |
| 夜防 | 工人危险圈 / 夜采安全 | 3 / 3（滞回脱离 5） |
| 夜防 | 出生点避让 | 4；黄昏提前 12 回合生效 |
| 夜防 | 开拓者撤离 | CP 贴脸 1 格 |
| 升级 | 应急金 / 基地门槛 | 30 / 250 |
| 维修 | 抢修血量 | <50%；武器未 L2 时临界墙 <30% |
| 任务 | 每日非任务 LLM | 3 |
| 任务 | 任务期 LLM 循环 | `min(8, timeout-2)` |
| 工程质量 | 内部异常 | 合法空响应 `{"roleCommandMap":{}}` |
| 遥测 | 每回合 stdout | 精简加密 Request+Response+trace，密钥 `12345678` |

---

## 11. 仍待实验 / 未完成

| 项 | 状态 | 说明 |
|---|---|---|
| 3 火箭 vs 2 火箭+1 电磁 | EXPERIMENT | 现用 3 火箭；需 Replay/实战 A/B |
| 夜采白名单 | 未开放 | U6（机器人是否绕墙）验证后再放开 |
| 动态 FRONT 重建 | 仅记录不拆炮重摆 | 拆 3 火箭成本太高 |
| 宝藏主线 | 框架已有 | 推断质量与收益量级未知 |
| 召唤令 PvP | 关闭 | 确认防御优势后再开 |
| 应急炸弹/眩晕 | 未做 | CRITICAL 时动用 `RESERVE_GOLD` |
| ReplayEngine | 未做 | 「先 Replay 再实战」的调参闭环 |

---

## 12. 配套文档

| 文档 | 职责 |
|---|---|
| 本文 `当前策略.md` | 当前怎么打（可读手册） |
| `STRATEGY_DECISIONS.md` | 策略参数真源，与代码常量一一对应 |
| `GAME_STRATEGY_DESIGN.md` | 初版「怎么赢」分析与三套备选（部分结论已被实战修正） |
| `RULE_ASSUMPTIONS.md` | 规则事实 / 冲突 / UNKNOWN |
| `自进化策略.md` | 任务求解器状态机细节 |
| `ARCHITECTURE_DESIGN.md` | 代码分层与模块边界 |
| `PROGRESS.md` | 工程进度、实战修复账本、待办 |
| `LOG_ANALYSIS_PLAYBOOK.md` | PK 后解密日志复盘 SOP |

策略改动必须走：观察 → 诊断 → 假设 → 最小改动 → 测试 → 度量 → KEEP / REVERT / INCONCLUSIVE。不要输一局就推翻整套打法。
